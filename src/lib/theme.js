import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: "YuPearl-Regular, sans-serif",
    body: "YuPearl-Regular, sans-serif",
  },
});

export default theme;
